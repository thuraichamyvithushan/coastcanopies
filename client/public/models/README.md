# Coast Canopies GLB assets

Place the exported Meshy `.glb` files in this directory using the exact filenames in `src/config/productConfig.js`.

Alignment values live in `src/config/modelTransforms.js`. The configurator displays placeholder meshes whenever an asset is not present.
